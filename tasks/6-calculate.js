/**
 * Применяет математическую операцию к каждому элементу массива
 * @param {number[]} numbers - Массив чисел
 * @param {Function} callback - Функция, применяющая математическую операцию
 * @returns {number[]} Массив с результатами операций
 */
function calculate(numbers, callback) {
  // Твой код здесь
}

module.exports = calculate; 