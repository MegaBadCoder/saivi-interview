/**
 * Полностью уплощает массив любой вложенности
 * @param {Array} arr - Исходный массив
 * @returns {Array} Уплощенный массив
 */
function myFlat(arr) {
  // Твой код здесь
}

module.exports = myFlat; 