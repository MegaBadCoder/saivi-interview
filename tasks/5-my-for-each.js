/**
 * Реализация метода forEach для массивов
 * @param {Array} arr - Массив для итерации
 * @param {Function} callback - Функция, вызываемая для каждого элемента
 * @param {Object} thisArg - Объект, который будет использоваться как this при вызове callback
 * @returns {void}
 */
function myForEach(arr, callback, thisArg) {
  // Твой код здесь
}

module.exports = myForEach; 