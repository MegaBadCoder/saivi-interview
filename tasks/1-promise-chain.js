/**
 * Выполняет последовательные запросы к массиву URL
 * @param {string[]} urls - Массив URL для запроса
 * @returns {Promise<any[]>} Массив результатов в том же порядке, что и URL
 */
function fetchSequentially(urls) {
  // Твой код здесь
}

module.exports = fetchSequentially; 