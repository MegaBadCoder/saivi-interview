/**
 * Объект калькулятора с базовыми операциями
 */
const calculator = {
  value: 0,
  add(x) {
    // Твой код здесь
  },
  subtract(x) {
    // Твой код здесь
  },
  multiply(x) {
    // Твой код здесь
  },
  divide(x) {
    // Твой код здесь
  },
  getResult() {
    // Твой код здесь
  },
  clear() {
    // Твой код здесь
  }
};

/**
 * Выполняет операцию калькулятора с правильным контекстом
 * @param {Function} operation - Метод калькулятора
 * @param {number} x - Операнд
 * @returns {object} Объект калькулятора для цепочки вызовов
 */
function executeOperation(operation, x) {
  // Твой код здесь
}

module.exports = { calculator, executeOperation }; 